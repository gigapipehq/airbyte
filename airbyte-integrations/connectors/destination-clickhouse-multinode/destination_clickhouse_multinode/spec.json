{
  "documentationUrl": "https://docs.airbyte.io/integrations/destinations/clickhouse-multinode",
  "supported_destination_sync_modes": [
    "overwrite", "append", "append_dedup"
  ],
  "supportsIncremental": true,
  "supportsDBT": false,
  "supportsNormalization": false,
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Destination Clickhouse Multinode Spec",
    "type": "object",
    "required": ["host", "port", "cluster", "database", "username"],
    "additionalProperties": true,
    "properties": {
      "host": {
        "title": "Host",
        "description": "Hostname of the database.",
        "type": "string",
        "order": 0
      },
      "cluster": {
        "title": "Cluster",
        "description": "The name of the cluster to connect to.",
        "type": "string",
        "examples": ["8123"],
        "order": 1
      },
      "port": {
        "title": "Port",
        "description": "JDBC port (not the native port) of the database.",
        "type": "integer",
        "minimum": 0,
        "maximum": 65536,
        "default": 8123,
        "examples": ["8123"],
        "order": 2
      },
      "tcp-port": {
        "title": "Native Port",
        "description": "Native port (not the JDBC) of the database.",
        "type": "integer",
        "minimum": 0,
        "maximum": 5536,
        "default": 9000,
        "examples": ["9000"],
        "order": 3
      },
      "database": {
        "title": "DB Name",
        "description": "Name of the database.",
        "type": "string",
        "order": 4
      },
      "username": {
        "title": "User",
        "description": "Username to use to access the database.",
        "type": "string",
        "order": 5
      },
      "password": {
        "title": "Password",
        "description": "Password associated with the username.",
        "type": "string",
        "airbyte_secret": true,
        "order": 6
      },
      "ssl": {
        "title": "SSL Connection",
        "description": "Encrypt data using SSL.",
        "type": "boolean",
        "default": false,
        "order": 7
      }
    }
  }
}
